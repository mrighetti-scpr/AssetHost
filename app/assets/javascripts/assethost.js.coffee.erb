###
Welcome to AssetHost
###

# stub console.log() for IE
if !window.console
    class window.console
        @log: ->

window.AssetHost ?= {}
AssetHost.SERVER = "<%= Rails.application.config.host %>"

# HACK
# AssetHost.PATH_PREFIX = "/a"
AssetHost.PATH_PREFIX = ""
AssetHost.SIZES =
    thumb:  "<%= Rails.application.config.thumb_size || "thumb" %>"
    modal:  "<%= Rails.application.config.modal_size || "modal" %>"
    detail: "<%= Rails.application.config.detail_size || "wide" %>"

    