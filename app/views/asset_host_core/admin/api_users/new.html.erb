<h2><%= @PAGE_TITLE = "Edit API User: #{@api_user.name}" %></h2>

<%= simple_form_for [:a, @api_user] do |f| %>
    <fieldset>
        <%= render 'form_fields', f: f %>

        <div class="form-actions">
            <%= f.submit "Save", class: "btn" %>
        </div>
    </fieldset>
<% end %>
